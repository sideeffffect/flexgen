language: python
python:
  - "3.4"
install:
  - virtualenv ~/flexget/
  - cd ~/flexget
  - bin/pip install flexget
script:
  - ./flexgen > $HOME/.config/flexget/config.yml
  - ~/flexget/bin/flexget check

